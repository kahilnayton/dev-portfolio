import React from 'react'
import {Inner} from '../styles/structure'

const Bio = (props) => {
  
  return (
    <Inner>
      {/* Write a bio with the content from prismic Bio */}
    </Inner>
  )
}

export default Bio;
