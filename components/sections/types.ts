export type FeaturedSectionProps = {
    heading: string
    content: string
     buttonText?: string
      destination?: string
        variant: string
  }

  export type ProjectProps = {
    projects: any[]
  }

  export type BlogProps = {
    blogs: any[]
  }